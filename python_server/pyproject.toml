# ── Tool configuration only (not a buildable package) ──────────────────────
# Dependencies are managed via requirements.txt

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"
